<% if (title) { %> 
    <h1><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>

    <% messages() %>

    <div>
      <a href="/inv/add-classification">Add new classification</a>
      <a href="/inv/add-inventory">Add new inventory view</a>
  </div>
 